<h1>Patients</h1><br>

<% @patient.appointments.each do |appointment| %>
  <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %> - <%= appointment.appointment_datetime.strftime("%B %e, %Y at %H:%M") %><br>
<% end %>